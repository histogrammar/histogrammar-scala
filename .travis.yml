language: java   # because we use Maven

os:
  - linux

jdk:
  - oraclejdk7
  - oraclejdk8
  - openjdk7

env:
  matrix:
    - SCALA_VERSION=Scala-2.10
    - SCALA_VERSION=Scala-2.11

script: "mvn clean install -P ${SCALA_VERSION}"
